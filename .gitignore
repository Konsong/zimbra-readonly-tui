# Personal Claude Code settings. Permission grants here are tied to one
# contributor's own GitHub credentials, so they stay out of the repository:
# checked in, they would hand every future clone the same rights silently.
# Team-wide settings, if they are ever needed, belong in .claude/settings.json.
.claude/settings.local.json
